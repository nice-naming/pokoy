export function roundToTenth(average: number): number {
  return Math.round(average * 10) / 10
}

export function roundToHundredth(average: number): number {
  return Math.round(average * 100) / 100
}
